# 🏗️ Admin Dashboard - Architecture

## System Overview

```
┌─────────────────────────────────────────────────────────────────┐
│                     PC Builder Admin Dashboard                   │
│                     http://localhost:3001                        │
└─────────────────────────────────────────────────────────────────┘
                                │
                                ▼
┌─────────────────────────────────────────────────────────────────┐
│                        Next.js 16 App Router                     │
│                         (TypeScript)                             │
└─────────────────────────────────────────────────────────────────┘
        │                       │                       │
        ▼                       ▼                       ▼
┌─────────────┐       ┌─────────────┐       ┌─────────────┐
│   UI Layer  │       │ State Layer │       │  API Layer  │
│ (shadcn/ui) │◄─────►│ React Query │◄─────►│    tRPC     │
└─────────────┘       └─────────────┘       └─────────────┘
                                                     │
                                                     ▼
                                            ┌─────────────┐
                                            │  @repo/api  │
                                            └─────────────┘
                                                     │
                                                     ▼
                                            ┌─────────────┐
                                            │  Prisma ORM │
                                            └─────────────┘
                                                     │
                                                     ▼
                                            ┌─────────────┐
                                            │  MySQL DB   │
                                            └─────────────┘
```

## 📂 Folder Architecture

```
apps/admin/
│
├── app/                              # Next.js App Router
│   │
│   ├── (dashboard)/                  # Route Group (shared layout)
│   │   │
│   │   ├── components/               # Layout Components
│   │   │   ├── Sidebar.tsx           # Navigation sidebar
│   │   │   └── Header.tsx            # Top header bar
│   │   │
│   │   ├── dashboard/                # Dashboard Route
│   │   │   └── page.tsx              # /dashboard
│   │   │       ├── Stats Cards
│   │   │       ├── Distribution Chart
│   │   │       ├── Recent Activity
│   │   │       └── Quick Actions
│   │   │
│   │   ├── components/               # Components Route
│   │   │   └── page.tsx              # /components
│   │   │       ├── Search & Filter
│   │   │       ├── Data Table
│   │   │       ├── Add Dialog
│   │   │       └── Edit/Delete Actions
│   │   │
│   │   ├── products/                 # Products Route
│   │   │   └── page.tsx              # /products (placeholder)
│   │   │
│   │   ├── settings/                 # Settings Route
│   │   │   └── page.tsx              # /settings
│   │   │       ├── General
│   │   │       ├── Profile
│   │   │       ├── Notifications
│   │   │       ├── Appearance
│   │   │       └── Security
│   │   │
│   │   └── layout.tsx                # Dashboard Layout Wrapper
│   │       ├── Sidebar (left)
│   │       └── Header + Content (right)
│   │
│   ├── components/                   # Shared Components
│   │   └── core/
│   │       └── trpc-provider.tsx     # tRPC React Query provider
│   │
│   ├── hooks/                        # Custom React Hooks
│   │   └── api/
│   │       └── useComponent.ts       # Component API hooks
│   │
│   ├── utils/                        # Utility Functions
│   │   └── api.ts                    # tRPC client setup
│   │
│   ├── globals.css                   # Global styles + Tailwind
│   ├── layout.tsx                    # Root layout
│   └── page.tsx                      # Home (redirects to /dashboard)
│
├── public/                           # Static Assets
│   └── favicon.ico
│
└── *.md                              # Documentation files
```

## 🔄 Data Flow

### Component List (Read)

```
User visits /components
        │
        ▼
┌───────────────────────────────────────────────────────────┐
│ page.tsx                                                  │
│   - useListComponents() hook                             │
└───────────────────────────────────────────────────────────┘
        │
        ▼
┌───────────────────────────────────────────────────────────┐
│ hooks/api/useComponent.ts                                 │
│   - api.components.list.useQuery()                        │
│   - staleTime: 5 minutes                                  │
│   - refetchOnWindowFocus: false                           │
└───────────────────────────────────────────────────────────┘
        │
        ▼
┌───────────────────────────────────────────────────────────┐
│ React Query Cache                                         │
│   - Caches data for 5 minutes                             │
│   - Returns cached data if available                      │
└───────────────────────────────────────────────────────────┘
        │
        ▼ (if cache miss)
┌───────────────────────────────────────────────────────────┐
│ tRPC Client                                               │
│   - utils/api.ts                                          │
│   - Calls @repo/api                                       │
└───────────────────────────────────────────────────────────┘
        │
        ▼
┌───────────────────────────────────────────────────────────┐
│ @repo/api (tRPC Router)                                   │
│   - components.list procedure                             │
│   - Returns typed data                                    │
└───────────────────────────────────────────────────────────┘
        │
        ▼
┌───────────────────────────────────────────────────────────┐
│ Prisma Client                                             │
│   - db.component.findMany()                               │
└───────────────────────────────────────────────────────────┘
        │
        ▼
┌───────────────────────────────────────────────────────────┐
│ MySQL Database                                            │
│   - Components table                                      │
└───────────────────────────────────────────────────────────┘
        │
        ▼ (returns data)
Components displayed in Table
```

### Component Create (Write - TODO)

```
User clicks "Add Component"
        │
        ▼
Dialog opens with form
        │
        ▼
User fills form & submits
        │
        ▼
┌───────────────────────────────────────────────────────────┐
│ handleAddComponent()                                      │
│   - TODO: Validate with Zod                               │
│   - TODO: Call createMutation.mutateAsync()               │
└───────────────────────────────────────────────────────────┘
        │
        ▼
┌───────────────────────────────────────────────────────────┐
│ tRPC Mutation (TODO)                                      │
│   - api.components.create.useMutation()                   │
└───────────────────────────────────────────────────────────┘
        │
        ▼
Database updated
        │
        ▼
Cache invalidated & refetch
        │
        ▼
Table updates automatically
```

## 🎨 Component Hierarchy

```
Root Layout
│
├── TRPCProvider
│   │
│   └── Dashboard Layout
│       │
│       ├── Sidebar
│       │   ├── Logo
│       │   ├── Navigation Links
│       │   │   ├── Dashboard
│       │   │   ├── Components
│       │   │   ├── Products
│       │   │   └── Settings
│       │   └── Logout Button
│       │
│       └── Main Content Area
│           │
│           ├── Header
│           │   ├── Search Bar
│           │   ├── Notification Bell
│           │   └── User Dropdown
│           │       ├── Profile
│           │       ├── Settings
│           │       └── Logout
│           │
│           └── Page Content
│               │
│               ├── Dashboard Page
│               │   ├── Stats Grid (4 cards)
│               │   ├── Charts Row (2 cards)
│               │   └── Quick Actions
│               │
│               ├── Components Page
│               │   ├── Search & Filter Bar
│               │   ├── Data Table
│               │   │   ├── Table Header
│               │   │   ├── Table Rows
│               │   │   └── Action Dropdown
│               │   ├── Add Dialog
│               │   └── Edit Dialog
│               │
│               ├── Products Page
│               │   └── Coming Soon
│               │
│               └── Settings Page
│                   ├── General Settings Card
│                   ├── Profile Settings Card
│                   ├── Notifications Card
│                   ├── Appearance Card
│                   └── Security Card
```

## 🔌 API Layer

```
┌─────────────────────────────────────────────────────────────┐
│                    tRPC API Endpoints                        │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  components.*                                                │
│  ├── list        GET     List all components         ✅      │
│  ├── create      POST    Create component            ⏳      │
│  ├── update      PUT     Update component            ⏳      │
│  └── delete      DELETE  Delete component            ⏳      │
│                                                              │
│  products.*                                                  │
│  ├── list        GET     List all products           ⏳      │
│  ├── create      POST    Create product              ⏳      │
│  ├── update      PUT     Update product              ⏳      │
│  └── delete      DELETE  Delete product              ⏳      │
│                                                              │
│  auth.*                                                      │
│  ├── login       POST    User login                  ⏳      │
│  ├── logout      POST    User logout                 ⏳      │
│  └── session     GET     Get session                 ⏳      │
│                                                              │
└─────────────────────────────────────────────────────────────┘

Legend: ✅ Implemented  ⏳ Planned  ❌ Not started
```

## 🗄️ Database Schema

```
┌─────────────────────────────────────────────────────────────┐
│                     Component Table                          │
├─────────────────────────────────────────────────────────────┤
│ id              INT          PRIMARY KEY, AUTO_INCREMENT     │
│ name            VARCHAR      Component name                  │
│ type            ENUM         Component type (CPU, GPU...)    │
│ price           FLOAT        Price in USD                    │
│ specifications  JSON         Technical specs                 │
│ createdAt       DATETIME     Creation timestamp              │
│ updatedAt       DATETIME     Last update timestamp           │
└─────────────────────────────────────────────────────────────┘

Component Types (ENUM):
- CPU
- MOTHERBOARD
- RAM
- STORAGE
- GPU
- PSU
- CASE
- COOLING
```

## 🎯 State Management

```
┌─────────────────────────────────────────────────────────────┐
│                     State Layers                             │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  Server State (React Query)                                  │
│  ├── Components list                                         │
│  ├── Cache: 5 minutes                                        │
│  ├── Auto refetch: disabled                                  │
│  └── Invalidation: manual on mutations                       │
│                                                              │
│  Local UI State (useState)                                   │
│  ├── Search query                                            │
│  ├── Filter type                                             │
│  ├── Dialog open/close                                       │
│  ├── Form data                                               │
│  └── Selected component                                      │
│                                                              │
│  Global State (Context - optional)                           │
│  ├── Theme preference                                        │
│  ├── User session                                            │
│  └── Language preference                                     │
│                                                              │
└─────────────────────────────────────────────────────────────┘
```

## 🎨 UI Component Dependencies

```
shadcn/ui Components (@repo/ui)
│
├── button.tsx           ─── @radix-ui/react-slot
├── card.tsx             ─── (no dependencies)
├── table.tsx            ─── (no dependencies)
├── dialog.tsx           ─── @radix-ui/react-dialog
├── input.tsx            ─── (no dependencies)
├── label.tsx            ─── @radix-ui/react-label
├── select.tsx           ─── @radix-ui/react-select
├── textarea.tsx         ─── (no dependencies)
├── badge.tsx            ─── (no dependencies)
├── avatar.tsx           ─── @radix-ui/react-avatar
├── dropdown-menu.tsx    ─── @radix-ui/react-dropdown-menu
├── separator.tsx        ─── @radix-ui/react-separator
└── scroll-area.tsx      ─── @radix-ui/react-scroll-area

All components use:
- clsx + tailwind-merge (cn utility)
- class-variance-authority (variants)
- Tailwind CSS (styling)
```

## 🚀 Build Process

```
┌─────────────────────────────────────────────────────────────┐
│                    Build Pipeline                            │
└─────────────────────────────────────────────────────────────┘
        │
        ▼
┌─────────────────────────────────────────────────────────────┐
│ 1. TypeScript Compilation                                   │
│    - Type checking                                           │
│    - Generate .d.ts files                                    │
└─────────────────────────────────────────────────────────────┘
        │
        ▼
┌─────────────────────────────────────────────────────────────┐
│ 2. Next.js Build (Turbopack)                                │
│    - Page compilation                                        │
│    - Route generation                                        │
│    - Static optimization                                     │
└─────────────────────────────────────────────────────────────┘
        │
        ▼
┌─────────────────────────────────────────────────────────────┐
│ 3. Tailwind CSS Processing                                  │
│    - Parse @apply directives                                 │
│    - Generate utility classes                                │
│    - Minify CSS                                              │
└─────────────────────────────────────────────────────────────┘
        │
        ▼
┌─────────────────────────────────────────────────────────────┐
│ 4. Bundle Optimization                                       │
│    - Code splitting                                          │
│    - Tree shaking                                            │
│    - Minification                                            │
└─────────────────────────────────────────────────────────────┘
        │
        ▼
┌─────────────────────────────────────────────────────────────┐
│ Output: .next/                                               │
│    ├── static/        (Static assets)                        │
│    ├── server/        (Server bundles)                       │
│    └── cache/         (Build cache)                          │
└─────────────────────────────────────────────────────────────┘
```

## 📦 Package Dependencies

```
admin app
│
├── Dependencies
│   ├── next (16.0.1)
│   ├── react (19.2.0)
│   ├── react-dom (19.2.0)
│   ├── tailwindcss (4.0.0)
│   ├── @tanstack/react-query (5.90.10)
│   ├── @trpc/next (11.7.1)
│   ├── @trpc/react-query (11.7.1)
│   ├── lucide-react (0.554.0)
│   ├── superjson (2.2.5)
│   └── tw-animate-css (1.4.0)
│
├── Workspace Dependencies
│   ├── @repo/ui (workspace:*)
│   ├── @repo/api (workspace:*)
│   └── @repo/db (workspace:*)
│
└── Dev Dependencies
    ├── typescript (5.9.2)
    ├── @types/react (19.2.2)
    ├── @types/react-dom (19.2.2)
    ├── @types/node (22.15.3)
    ├── eslint (9.39.1)
    ├── @repo/eslint-config (workspace:*)
    └── @repo/typescript-config (workspace:*)
```

## 🔐 Security Architecture

```
┌─────────────────────────────────────────────────────────────┐
│                   Security Layers (TODO)                     │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  1. Authentication                                           │
│     ├── Session-based auth                                   │
│     ├── JWT tokens                                           │
│     └── Secure cookie storage                                │
│                                                              │
│  2. Authorization                                            │
│     ├── Role-based access control (RBAC)                     │
│     ├── Admin role required                                  │
│     └── Protected API routes                                 │
│                                                              │
│  3. Data Validation                                          │
│     ├── Zod schemas                                          │
│     ├── Input sanitization                                   │
│     └── XSS prevention                                       │
│                                                              │
│  4. API Security                                             │
│     ├── CSRF protection                                      │
│     ├── Rate limiting                                        │
│     └── Request validation                                   │
│                                                              │
└─────────────────────────────────────────────────────────────┘
```

## 🎯 Performance Optimizations

```
┌─────────────────────────────────────────────────────────────┐
│                   Performance Strategy                       │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  1. Data Fetching                                            │
│     ├── React Query caching (5min)                           │
│     ├── Disabled refetch on focus                            │
│     └── Manual invalidation                                  │
│                                                              │
│  2. Rendering                                                │
│     ├── Server Components (where possible)                   │
│     ├── Client Components (interactive)                      │
│     └── Code splitting per route                             │
│                                                              │
│  3. Styling                                                  │
│     ├── Tailwind JIT compilation                             │
│     ├── CSS purging                                          │
│     └── Minimal runtime                                      │
│                                                              │
│  4. Bundle Size                                              │
│     ├── Tree shaking                                         │
│     ├── Dynamic imports                                      │
│     └── Shared chunks                                        │
│                                                              │
└─────────────────────────────────────────────────────────────┘
```

---

**Last Updated:** 2024  
**Version:** 1.0.0  
**Architecture Status:** ✅ Documented